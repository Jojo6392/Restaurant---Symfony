{% extends 'base.html.twig' %}

{% block title %}Ligne de commande{% endblock %}

{% block body %}
    <h1>Ligne {{ ligne_de_commande.id }} - Commande n°{{ ligne_de_commande.commande.numero }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ligne_de_commande.id }}</td>
            </tr>
            <tr>
                <th>Produit</th>
                <td>{{ ligne_de_commande.produit.nom }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ ligne_de_commande.quantite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('commande_index', {'id': app.user.id }) }}">Retour aux commandes</a>
    <br />
    <a href="{{ path('ligne_de_commande_edit', {'id': ligne_de_commande.id}) }}">Modifier</a>

    {{ include('ligne_de_commande/_delete_form.html.twig') }}
{% endblock %}
