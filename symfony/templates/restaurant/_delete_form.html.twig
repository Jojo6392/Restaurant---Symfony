<form method="post" action="{{ path('restaurant_delete', {'id': restaurant.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce restaurant ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ restaurant.id) }}">
    <button class="btn">Supprimer <img class="icons" src="{{ asset('assets/delete.png') }}" /></button>
</form>
